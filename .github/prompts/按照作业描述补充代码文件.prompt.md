---
name: 按照作业描述补充代码文件
description: 按照作业描述补充代码文件
---
请根据仓库中的作业 handout（MinerU_markdown_cs336_spring2025_assignment1_basics_2022353073144655872.md）和当前代码结构，为指定 Problem 创建“仅接口模板代码”，不要实现完整逻辑。

目标：
1) 严格按 handout 给出的 class / function 签名创建文件；
2) 只写骨架 + TODO 注释，保留我后续补全空间；
3) 使用项目现有风格（Python 3.11、torch、jaxtyping）；
4) 将新类加入对应子包的 __init__.py 导出；
5) 不创建测试文件、不改 README、不写额外文档。

代码要求：
- 子包路径：cs336_basics/nn/ 或 cs336_basics/tokenizer/（按功能归类）。
- 必须包含类型注解；张量使用 jaxtyping 形状注解（如 Float[Tensor, "... d_model"]）。
- 参数命名、默认值、返回类型必须与 handout 一致。
- forward/body 中仅保留 TODO 和必要的占位（如 raise NotImplementedError）。
- 不使用 try/except，不加兜底默认行为。

执行步骤：
- 先读取 handout 中对应 Problem 段落，提取推荐 interface。
- 再检查现有目录与 __init__.py，按当前结构落位。
- 编辑完成后检查目标文件无语法错误。
- 最后简要汇报：新增/修改了哪些文件，以及我需要补全哪些 TODO。

本次要处理的 Problem：